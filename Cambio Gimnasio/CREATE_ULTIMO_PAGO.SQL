create table PAGO_APARATOS(
       CLIENTE INTEGER,
       FECHA_ULTIMO_PAGO DATE,
       FECHA_EFECTIVO DATE,
       DISIPLINA INTEGER
);

ALTER TABLE PAGO_APARATOS ADD CONSTRAINT FK_ULTIMO_PAGO FOREIGN KEY(CLIENTE) REFERENCES CLIENTES (ID_CLIENTE);


